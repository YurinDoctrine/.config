#!/usr/bin/sh
if [ "$(pidof polybar)" ]; then
    (killall -HUP polybar && polybar -q main top -c ~/.config/polybar/config.ini) &
    exit
else
    polybar -q main top -c ~/.config/polybar/config.ini &
    exit
fi
