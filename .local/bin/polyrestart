#!/usr/bin/sh
if [ "$(pidof polybar)" ]; then
    (killall -HUP polybar && polybar -q main -c ~/.config/polybar/config.ini) &
else
    exec polybar -q main -c ~/.config/polybar/config.ini &
    renice -n 19 -p $(pidof polybar)
fi
