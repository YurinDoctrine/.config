#!/usr/bin/sh
if [ "$(pidof polybar)" ]; then
    (killall polybar && sleep 0.1 && polybar -q main -c ~/.config/polybar/shades/config.ini) &
    exit 0
else
    polybar -q main -c ~/.config/polybar/shades/config.ini &
    exit 0
fi
